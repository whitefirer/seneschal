name: Test Description
version: "1.0"

steps:
  - name: step-1
    description: "这是一个测试步骤的描述信息"
    action: shell
    shell: bash
    command: echo -e "\033[1;32mStep 1 completed\033[0m"

  - name: step-2
    description: "验证环境变量是否正确设置"
    action: shell
    command: echo "Step 2 done"

  - name: step-3
    action: shell
    command: echo "Step 3 without description"