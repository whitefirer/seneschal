name: test-parallel-infer
description: 测试 Parallel 依赖推断

steps:
  - name: parallel-tasks
    action: parallel
    steps:
      - name: task1
        action: shell
        command: echo "Task 1"
      - name: task2
        action: shell
        command: echo "Task 2"
      - name: task3
        action: shell
        command: echo "Task 3"
        
  - name: merge
    action: shell
    command: echo "All tasks completed"
